types Default
# ^ keyword
{
	type shipping_lane_information = flowcontainer {
	# 		^ class
		parentanchor = hcenter
		# ^ field.name
		#				
		margin_left = 10
		# ^ field.name
		#              ^ number
		minimumsize = { @panel_width -1 }
		# ^ field.name
		#              		^ ref
		#							  
	}
}